dist: xenial
language: python
python:
  - 3.5
  - 3.6
  - 3.7
  - 3.8
  - 3.9

install:
  - pip install .
  - pip install -r requirements.txt

script:
  - mkdir empty
  - cd empty
  - python -m pyepsg -v
